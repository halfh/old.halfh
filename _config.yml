host: 0.0.0.0

collections:
  authors:
    output: true

site:
  theme_config:
    appearence: dark
    
defaults:
  - scope:
      path: ""
      type: "authors"
    values:
      layout: "author"
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
  - scope:
      path: ""
    values:
      layout: "default"